#ifndef DESTINATION_H
#define DESTINATION_H

#include <string>

const int TOTAL_DESTINATION = 47;

void displayDestinations(const std::string destinations[]);
std::string selectDestination(const std::string destinations[]);

#endif